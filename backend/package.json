{
  "name": "database-server",
  "version": "0.0.0",
  "devDependencies": {
    "wrangler": "^2.1.15"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "@tsndr/cloudflare-worker-router": "^2.2.0",
    "@upstash/redis": "^1.16.0",
    "qr-image": "^3.2.0"
  }
}
